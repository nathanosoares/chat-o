{
  "name": "chat-o",
  "version": "1.0.0",
  "scripts": {
    "compile": "tsc -b tsconfig.build.json",
    "start:server": "npm run start --workspace=@chat-o/server",
    "start:web": "npm run start --workspace=@chat-o/web"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.7.14",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "buffer-stream-js": "^1.0.0"
  }
}
