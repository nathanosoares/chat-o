{
  "name": "chat-o",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "tsc -b tsconfig.build.json",
    "start": "npm run start --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.7.14",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.2"
  }
}
